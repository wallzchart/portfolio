/* 🌌 Solid Sky Blue Background */
body {
    background-color: #87CEEB; /* Sky blue */
    font-family: "Courier New", monospace; /* Command Prompt Style */
    color: #d4ffb3; /* Light green text */
    text-align: center;
    padding: 0;
    margin: 0;
}

/* 🖥️ Windows XP-Style Title Bar */
.title-bar {
    background: linear-gradient(to right, #004080, #0080ff);
    color: white;
    font-size: 24px;
    padding: 10px;
    text-align: left;
    font-weight: bold;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

/* 📁 Title Bar Buttons */
.title-buttons button {
    background: lightgray;
    border: 1px solid black;
    padding: 2px 6px;
    margin: 2px;
    cursor: pointer;
}

/* 📂 Windows XP-Style Navigation */
.nav {
    background: #008080;
    padding: 10px;
    border-bottom: 4px solid #004080;
    text-align: left;
    font-size: 18px;
}

.nav-section a {
    color: white;
    text-decoration: none;
    padding: 10px;
}

.nav-section a:hover {
    text-decoration: underline;
}

/* 🔄 Diagonal Layout for Projects */
.content {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    margin: 20px;
    padding: 20px;
}

/* 📦 Windows XP-Style Section Boxes */
.section {
    width: 45%;
    padding: 20px;
    background: rgba(0, 0, 0, 0.8);
    border: 3px solid #d4ffb3;
    box-shadow: 5px 5px 15px #00ffcc;
    border-radius: 10px;
    text-align: center;
}

/* 📜 Text Box Formatting */
.text-box {
    background: rgba(0, 0, 0, 0.5);
    padding: 15px;
    border-radius: 10px;
    margin-top: 10px;
}

/* 🎥 Video Styling */
iframe {
    width: 90%;
    border-radius: 8px;
    box-shadow: 5px 5px 15px #00ffcc;
}

/* 🖱️ Custom Cursor */
body { 
    cursor: url('https://raw.githubusercontent.com/wallzchart/portfolio/refs/heads/main/Cursor.cur'), auto; 
}

/* 🔄 Positioning */
.left {
    text-align: left;
}

.right {
    text-align: right;
}
