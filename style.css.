/* 🌈 Background */
body {
    background: url('https://i.imgur.com/JGzF01U.gif'); /* Sparkling glitter background */
    font-family: "Trebuchet MS", Verdana, Geneva, sans-serif; /* 90s-era web fonts */
    color: #ff33cc; /* Bright pink text */
    text-align: center;
}

/* 🖥️ Main content box */
.container {
    background: linear-gradient(45deg, rgba(0, 0, 0, 0.9), rgba(50, 0, 50, 0.8));
    padding: 20px;
    border: 5px solid #ff33cc;
    box-shadow: 0px 0px 20px #ff33cc, 0px 0px 40px cyan;
    width: 85%;
    margin: auto;
    border-radius: 15px;
}

/* 🎭 Navigation */
.nav a {
    font-size: 20px;
    color: yellow;
    text-decoration: none;
    margin: 10px;
}

.nav a:hover {
    color: white;
    text-decoration: underline;
}

/* 📌 Artistic Layout for Projects */
.project-gallery {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
    align-items: center;
    gap: 30px;
    margin-top: 30px;
}

/* 🔥 Individual Project Styling */
.project {
    width: 45%;
    padding: 20px;
    border: 5px solid cyan;
    box-shadow: 5px 5px 15px magenta;
    border-radius: 10px;
    transition: transform 0.2s ease-in-out;
    background: rgba(0, 0, 0, 0.8);
}

/* 🔄 Slight tilt effect when hovered */
.project:hover {
    transform: rotate(-3deg) scale(1.05);
}

/* 🖼️ Culture Jam Image */
.project1 img {
    width: 100%;
    border: 5px solid yellow;
    box-shadow: 5px 5px 15px cyan;
    border-radius: 10px;
}

/* 🎬 Video Remix */
.project2 iframe {
    width: 100%;
    border-radius: 10px;
    box-shadow: 5px 5px 15px lime;
}

/* 📜 Artist Statement */
.artist-statement {
    font-size: 18px;
    background: rgba(255, 255, 255, 0.2);
    padding: 10px;
    border-radius: 8px;
    color: white;
    text-shadow: 2px 2px 5px black;
}

/* 🖱️ Classic Windows Cursor */
body { 
    cursor: url('https://cur.cursors-4u.net/cursors/cur-2/cur126.cur'), auto; 
}

